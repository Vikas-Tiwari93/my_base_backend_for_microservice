// collection Seeding(from Dev to prod for first time);
export const collections = [
    "Users", "Admin"
]
export const SeedCollections = async () => {
    collections.forEach((elm) => {

    })
    return []
}

// Documents seeding(from Dev to prod for first time);